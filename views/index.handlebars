<div id="output">

  <div id="ready">
    <h2>Burgers ready to eat!</h2>

    <ul>
       {{#each burgers}} {{#unless dataValues.devoured}} {{> burgers/burger-block devour=true}} {{/unless}} {{/each}}
    </ul>
  </div>

  <div id="eaten">
    <h2>Eaten burgers!</h2>

    <ul>
      {{#each burgers}} {{#if dataValues.devoured}} {{> burgers/burger-block devour=false}} {{/if}} {{/each}}
    </ul>
  </div>

</div>